{{outlet}}

<h1>tasks </h1>
<!-- {{#each task in model}}
  <div class="well">
    <h4>{{task.title}}</h4>
    <p>{{task.descriptionm}}</p>
  </div>
{{/each}} -->

{{#each model as |task|}}
  <div class="well">
    <h4>{{task.title}}</h4>
    <small>Due: {{format-date task.date}}</small>
    <p>{{task.description}}</p>
  </div>
{{/each}}
